version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18.20.5
   
    commands:
        - npm install
       
  build:
    commands:
        # run build script
        - npm run build
artifacts:
  files:
    - '**/*'
  base-directory: . # This is the root directory where your index.html is
